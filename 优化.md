这是一份为你量身定制的**“爱情时间线”全本地化混合架构优化方案**。

这个方案的核心哲学是：**“能用代码解决的绝不上 AI，必须用 AI 的只用轻量级模型”。**

我们将结合 **UI 改造**（前端）、**CLIP 视觉检索**（后端核心）、**元数据逆地理编码**（辅助增强）三者，打造一个既快又聪明的本地回忆录。

---

### 1. 总体架构流程图
+-------------------------------------------------------+
|  [ 标题：记录美好时光 ]                                 |
+-------------------------------------------------------+
|  [ (选中)📷 照片 ]   [ 🎥 视频 ]   [ 📄 故事/文档 ]    |  <-- 1. 顶部类型切换
+-------------------------------------------------------+
|                                                       |
|   +-----------------------------------------------+   |
|   |                                               |   |
|   |         (大图标)                              |   |  <-- 2. 超大上传/拖拽区
|   |     点击或拖拽照片到这里 (支持批量)             |   |
|   |                                               |   |
|   +-----------------------------------------------+   |
|                                                       |
|   [ 预览图1 ] [ 预览图2 ] [ 预览图3 ] ...              |
+-------------------------------------------------------+
|  发生时间:  [ 2023/10/01 ]  (✨ 已自动识别)            |  <-- 3. 自动填充的日期
+-------------------------------------------------------+
|  文本内容:                                             |
|  [ 这一天我们在哪里？做了什么？...                  ]  |  <-- 4. 补充描述
|                                                       |
+-------------------------------------------------------+
|  标签 (自动分析/手动):                                  |
|  [ #旅游 x ]  [ #大理 x ]  ( + 添加 )                  |  <-- 5. 标签放最后
+-------------------------------------------------------+
|                [ ❤️ 保存记忆 ]                         |
+-------------------------------------------------------+

---

### 2. 前端交互层：所见即所得 (UI/UX)

按照之前的讨论，彻底改造上传界面，引导用户进行“结构化输入”。

#### A. 界面布局 (Layout)

1. **顶部导航栏 (Tabs)：**
* `[照片]` (默认): 激活 CLIP 流程。
* `[视频]` : 激活元数据 + 文件名流程。
* `[文件]` : 激活纯文本流程。


2. **中央上传区 (Dropzone)：**
* 巨大的虚线框，支持拖拽。
* *逻辑：* 根据 Tab 限制文件类型（如视频 Tab 下只能选 .mp4/.mov）。


3. **智能元数据栏 (Auto-fill Fields)：**
* `日期`：默认置灰。文件拖入后，JS 读取 Exif 自动填入 `2023-05-20`。
* `地点`：默认置灰。文件拖入后，JS 读取 GPS -> 调用离线库 -> 自动填入 `大理古城`。
* *用户干预：* 只有在自动识别失败时，用户才需要手动修改。


4. **描述框 (Description)：**
* 提示语：“这一刻发生了什么？”（用于补充 CLIP 看不懂的细节，比如“求婚成功的瞬间”）。

---

### 3. 后端处理层：混合引擎 (The Hybrid Engine)

这是最核心的部分。根据上传的文件类型，走不同的处理管道。

#### 管道 A：照片处理 (CLIP + Metadata)

* **适用：** `.jpg`, `.png`, `.heic`
* **步骤：**
1. **元数据提取 (Python `exifread`):** 拿到拍摄时间、GPS 坐标。
2. **逆地理编码 (Offline Geocoding):**
* 利用 Python 库 `reverse_geocoder` (离线库，几十 MB)。
* 将 `(25.69, 100.16)` 转换为文本 `中国, 云南省, 大理白族自治州`。


3. **视觉向量化 (CLIP):**
* 加载 `clip-ViT-B-32` 模型 (约 300MB，CPU 可跑)。
* 直接将**图片**输入模型，得到一个 `512维` 的向量。
* *注意：* 不需要把图片转成文字描述！CLIP 的神奇之处在于，你存的是“图片的向量”，但可以用“文字的向量”去搜它。


4. **存储：** 存入向量库。



#### 管道 B：视频处理 (Metadata Only)

* **适用：** `.mp4`, `.mov`
* **步骤：**
1. **元数据提取 (Python `ffmpeg-python`):** 极速读取视频创建时间、时长。
2. **文件名清洗：**
* 如果文件名是 `IMG_2934.mp4` -> 仅记录时间地点。
* 如果文件名是 `婚礼誓言.mp4` -> 将“婚礼誓言”作为关键词索引。


3. **不进行画面分析：** 节省大量算力。
4. **存储：** 存入 SQL 数据库（主要靠时间和地点检索）。


#### 管道 C：文档处理 (Text Embedding)

* **适用：** `.pdf`, `.txt`
* **步骤：**
1. **文本提取：** 读取文字内容。
2. **文本向量化：** 使用轻量级模型（如 `paraphrase-MiniLM-L6-v2`）生成向量。
3. **存储：** 存入向量库。

---

### 4. 数据存储设计 (Schema)

你需要两个库配合：**ChromaDB** (存向量) + **SQLite** (存结构化数据)。

**SQLite 表结构 (memories):**
| ID | type | path | date | location_text | filename | description |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | image | /data/img1.jpg | 2023-05-20 | 大理古城 | img1.jpg | "开心的合影" |
| 2 | video | /data/vid1.mp4 | 2023-05-20 | 大理古城 | 洱海.mp4 | NULL |

**ChromaDB 集合 (clip_vectors):**

* `ids`: ["1"] (对应 SQLite 的 ID)
* `embeddings`: [0.12, 0.33, ...] (CLIP 生成的图片向量)
* `metadatas`: {"type": "image"}

---

### 5. 搜索与聊天逻辑 (Retrieval & Chat)

当用户问：“找一下我们在大理结婚的照片”。

**Step 1: 意图解析 (Local LLM - Qwen2.5)**

* LLM 分析用户问题，提取关键信息：
* **关键词 (Query):** "结婚"
* **地点限制 (Filter):** "大理"
* **类型限制 (Filter):** "照片"



**Step 2: 执行混合搜索**

1. **SQL 过滤 (硬指标):**
* 先在 SQLite 里找：`SELECT id FROM memories WHERE type='image' AND location_text LIKE '%大理%'`
* 拿到候选 ID 列表：`[101, 102, 105, 200]`。


2. **向量搜索 (软语义):**
* 将关键词 "结婚" 输入 CLIP 文本编码器，得到向量。
* 在 ChromaDB 中搜索与该向量最相似的图片，且 **ID 必须在候选列表 [101...200] 中**。



**Step 3: 结果展示**

* 系统返回最匹配的 3 张图片路径。
* 聊天机器人回复：“找到了几张在大理结婚的照片，看看是这些吗？[图片1] [图片2]”

---

### 6. 技术栈推荐清单 (Python)

为了方便你直接让 AI 写代码，这里是精确的库名：

* **界面/API:** `FastAPI` (后端) + `Vue.js` (前端) 或直接用 `Streamlit` (快速原型)。
* **CLIP 模型:** `sentence-transformers` (加载 `clip-ViT-B-32`)。
* **图片元数据:** `exifread` 或 `Pillow`。
* **视频元数据:** `ffmpeg-python`。
* **逆地理编码:** `reverse_geocoder` (离线，速度极快) 或 `geopy` (需联网)。
* **向量数据库:** `chromadb`。
* **结构化数据库:** `sqlite3`。

### 7. 方案优势总结

1. **极速导入：** 视频不分析画面，照片用 CLIP（毫秒级），批量导入 1000 个文件也就是几分钟的事。
2. **精准定位：** 有了“逆地理编码”，你不用搜“某年某月”，直接搜“大理”、“海边”就能找到，这才是人类的记忆方式。
3. **完全免费且私密：** 不需要任何 API Key，断网也能跑，情侣隐私绝对安全。

**下一步行动建议：**
先让 Trae 写一个 **Python 脚本**，功能是：“遍历指定文件夹，用 CLIP 提取图片向量存入 ChromaDB，同时用 Exifread 提取经纬度并转为地名存入 SQLite”。先把这个后端核心跑通。